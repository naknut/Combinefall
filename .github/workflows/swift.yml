name: Swift

on: [push]

jobs:
  build:

    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set Xcode Version to 11.1
      run: sudo code-select --switch /Applications/Xcode_11.1.app/Contents/Developer
    - name: Run tests
      run: swift test -v
